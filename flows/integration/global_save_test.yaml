name: test_flow
version: "1.0.0"
description: "Test flow for unit tests"
on: manual

steps:
  - id: hello
    use: core.echo
    with:
      text: "Hello, World!"

  - id: goodbye
    use: core.echo
    with:
      text: "Goodbye, World!"
